[scan]
report_dir = "reports"
scanner_timeout_seconds = 300
max_output_bytes = 1048576
max_concurrency = 5
fail_on_severity = "high"

[budget]
max_context_tokens = 120000
warn_token_ratio = 0.8
hard_token_cap = 150000
budget_usd = 1.50
# Gemini free-tier default. Override these if you switch to paid-tier pricing.
input_cost_per_1k = 0
output_cost_per_1k = 0

[ai]
enabled = true
model = "gemini-3.5-flash"

# Secrets do not belong in this file.
# Put GEMINI_API_KEY and repository-specific secrets in .env or your shell environment.
